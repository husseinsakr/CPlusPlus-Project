/*
 * =================================
 * File: Game.cpp
 * 
 * Authors: 
 * Jordan Stanley (7432058)
 * jstan064@uottawa.ca
 * Hussein Sakr (7897706)
 * hsakr011@uottawa.ca
 * =================================
 */
#include <iostream>

#include "GameDetails.h"
/*
using std::cin;
using std::cout;
using std::endl;
using std::string;
using std::vector;

int main() {
	int* gameSelect;
	ScoreSheet* pGameSheet;
	int numPlayers;
	vector<string> players;
	string playerName = "";
	
	while (gameSelect != 1 || gameSelect != 2) {
		cout << "Hello User! Which roll the dice game would you like to play?" << "\n" << "1 for Qwinto, 2 for Qwixx: " << endl;
		cin >> gameSelect >> endl;
	}
	while (numPlayers != 1 || numPlayers != 2 || numPlayers != 3) {
		cout << "How many players are playing? Game supports either 1, 2, or 3 players: " << endl;
		cin >> numPlayers >> endl;
	}
	for (int i = 0; i < numPlayers; i++){
		cout <<"What is the name of Player " << i+1 << ": ";
		cin >>  playerName >> endl;
		players.push_back(playerName);
		
	}
	if (*gameSelect == 1) {
		pGameSelect = new QwintoScoreSheet;
	}
	else {
		pGameSelect = new QwixxScoreSheet;
	}
	
}
*/
